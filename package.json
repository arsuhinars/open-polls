{
  "name": "open-polls",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.ts",
  "scripts": {
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build",
    "build": "run-p build:*",
    "start": "cross-env NODE_ENV=production npm --prefix backend run start"
  },
  "author": "arsuhinars",
  "license": "MIT",
  "dependencies": {
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5"
  }
}
